apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Stub Secrets що kagent ADK pods читають при init.
# Applied EARLY (kagent-stubs Flux Kustomization, перед kagent-controller HelmRelease)
# щоб present до того як Helm install spawn'ить Agent CRDs → pods.
# Без цього splitу — deadlock: Helm чекає pods Ready, pods чекають Secret, Secret
# у kagent-config який чекає controller.
resources:
  - openai-stub.yaml
