apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # openai-stub.yaml moved to ../stubs/ — applied EARLY (kagent-stubs Flux
  # Kustomization). Reason: Helm install kagent-controller spawn'ить Agent
  # CRDs → pods що потребують Secret. Якщо Secret applied тут (after
  # controller) — deadlock на bootstrap.
  - model-config.yaml
  - qwen-model-config.yaml
  - agent-trust-patches.yaml
  - test-agents.yaml
