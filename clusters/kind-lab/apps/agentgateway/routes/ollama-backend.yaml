# Ollama backend — локальна Ollama на Mac host через docker bridge gateway.
#
# Архітектура:
#   pod всередині cluster
#     → Service "ollama" (headless, ClusterIP=None)
#     → EndpointSlice → 10.222.222.222:11434 (Mac host через OrbStack docker bridge)
#     → Ollama listening 0.0.0.0:11434 (виставлено через Ollama.app Settings або LaunchAgent)
#
# Чому headless Service + EndpointSlice: external IP не може бути ClusterIP target,
# K8s не дозволяє selectors на external адресах. Headless Service + manual EndpointSlice
# = pattern "external service abstraction".
#
# AgentgatewayBackend з provider=openai бо Ollama експонує /v1/chat/completions —
# OpenAI-compatible API.
---
apiVersion: v1
kind: Service
metadata:
  name: ollama
  namespace: agentgateway-system
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - port: 11434
      targetPort: 11434
      protocol: TCP
---
apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: ollama
  namespace: agentgateway-system
  labels:
    kubernetes.io/service-name: ollama
addressType: IPv4
endpoints:
  - addresses:
      - 10.222.222.222     # OrbStack docker bridge gateway = Mac host
ports:
  - port: 11434
    protocol: TCP
---
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: ollama
  namespace: agentgateway-system
spec:
  ai:
    provider:
      openai:
        model: "qwen2.5:7b"
      host: ollama.agentgateway-system.svc.cluster.local
      port: 11434
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: ollama
  namespace: agentgateway-system
spec:
  parentRefs:
    - name: agentgateway-proxy
      namespace: agentgateway-system
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /ollama
      # Strip '/ollama' префікс перш ніж forward upstream:
      # /ollama/chat/completions → /v1/chat/completions (Ollama OpenAI endpoint)
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /v1
      backendRefs:
        - name: ollama
          namespace: agentgateway-system
          group: agentgateway.dev
          kind: AgentgatewayBackend
