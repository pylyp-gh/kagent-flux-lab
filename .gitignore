# direnv — keep raw API keys out of git
.envrc

# Sealed Secrets RSA backups — NEVER commit these (contains cluster's private key)
*sealed-secrets-key*
*.tls.key

# kube state
*.kubeconfig
kubeconfig*

# OS / editor noise
.DS_Store
*.swp
*.swo
*~

# scratch
/.tmp/
*.log
